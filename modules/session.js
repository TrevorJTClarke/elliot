/**
 * Session
 * Handles all requests, verify we have IoT only devices
 */
var Session = function(req, res, next) {
    // TODO: finish some logic, woo!
    console.log('Something is happening.');
    next();
};

module.exports = Session;
